/**
 * Analytics mount point -- intentionally empty for Phase 1.
 *
 * Drop the provider snippet here (Plausible, Amplify/Vercel analytics, etc.) and
 * it loads site-wide from the root layout. Nothing else needs to change.
 */
export function Analytics() {
  return null;
}
